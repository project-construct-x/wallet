#################################################################################
#  Copyright (c) 2026 Zentralverband der Deutschen Elektro- und Informationstechnischen Handwerke (ZVEH)
#
#  See the NOTICE file(s) distributed with this work for additional
#  information regarding copyright ownership.
#
#  This program and the accompanying materials are made available under the
#  terms of the Apache License, Version 2.0 which is available at
#  https://www.apache.org/licenses/LICENSE-2.0.
#
#  Unless required by applicable law or agreed to in writing, software
#  distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
#  WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
#  License for the specific language governing permissions and limitations
#  under the License.
#
#  SPDX-License-Identifier: Apache-2.0
#################################################################################

{{ if .Values.vault.hashicorp.init.enabled -}}
{{- $fullName := .Values.fullnameOverride -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ $fullName }}-vault-init
  namespace: {{ .Release.Namespace | default "default" | quote }}
  labels:
    {{- include "wallet.labels" . | nindent 4 }}
  annotations:
    # ArgoCD ordering: run after Vault/Postgres (wave 0) are Healthy, but before the
    # wallet Deployment (wave 2), so the AES key exists before the runtime boots.
    # Replace=true recreates the Job on every sync (Jobs are otherwise immutable) and
    # lets a re-sync re-seed a dev-mode Vault that lost its in-memory secrets on restart.
    "argocd.argoproj.io/sync-wave": "1"
    "argocd.argoproj.io/sync-options": Replace=true
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 600
  activeDeadlineSeconds: 300
  template:
    metadata:
      labels:
        {{- include "wallet.labels" . | nindent 8 }}
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "wallet.serviceAccountName" . }}
      containers:
        - name: vault-init
          {{- $img := index .Values "vault" "hashicorp" "init" "image" | default dict }}
          image: {{ $img.repository | default "alpine" }}:{{ $img.tag | default "3.20" }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              tr -d '\r' < /scripts/init.sh > /tmp/init.sh
              exec sh /tmp/init.sh
          env:
            - name: VAULT_ADDR
              value: {{ tpl .Values.vault.hashicorp.url . | quote }}
            - name: VAULT_TOKEN
              value: {{ .Values.vault.hashicorp.token | required "vault.hashicorp.token is required" }}
            - name: VAULT_SECRET_PATH
              value: {{ .Values.vault.hashicorp.paths.secret | quote }}
            - name: VAULT_HEALTH_PATH
              value: {{ .Values.vault.hashicorp.paths.health | quote }}
            {{- with .Values.vault.hashicorp.init.forceRegenerate }}
            - name: FORCE_REGENERATE
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.vault.hashicorp.init.aesKeyAlias }}
            - name: AES_KEY_ALIAS
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.vault.hashicorp.init.privateKeyAlias }}
            - name: PRIVATE_KEY_ALIAS
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.vault.hashicorp.init.publicKeyAlias }}
            - name: PUBLIC_KEY_ALIAS
              value: {{ . | quote }}
            {{- end }}
          volumeMounts:
            - name: script
              mountPath: /scripts
              readOnly: true
      volumes:
        - name: script
          configMap:
            name: {{ $fullName }}-vault-init
            defaultMode: 0555
{{- end }}
